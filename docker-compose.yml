# version docker compose
version: '3.7'
services:
  app:
    container_name: react-app
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules' # nescessário para não sobrescrever o dir do build
    ports:
      - '3001:3000'
    environment:
      - NODE_ENV=development
      - CI=true